require 'rails_helper'

describe "adds a new review process" do
  it "adds a new review to a product" do
    # user = FactoryGirl.create(:user)
    # login_as(user, :scope => :user)
    # //logic for adding a comment
  end
end
